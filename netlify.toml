[build]
  functions = "netlify/functions"
  command = "cd netlify/functions && npm install && cd ../.."
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
